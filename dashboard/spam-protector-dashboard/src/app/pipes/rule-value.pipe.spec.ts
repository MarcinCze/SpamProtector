import { RuleValuePipe } from './rule-value.pipe';

describe('RuleValuePipe', () => {
  it('create an instance', () => {
    const pipe = new RuleValuePipe();
    expect(pipe).toBeTruthy();
  });
});
